cc = gcc
cflags = -Wall -lm -lraylib # for Linux
#cflags = -Wall -lraylib -framework Cocoa -framework IOKit -framework CoreVideo # for MacOS

rule cc
 command = $cc $cflags $in -o $out
 description = Build $in into $out

build main: cc main.c
